package ohai.newslang.repository.scrap;

import ohai.newslang.domain.entity.scrap.ScrapNewsArchive;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScrapNewsArchiveRepository extends JpaRepository<ScrapNewsArchive, Long> {
}
